<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bp.jaringochi.domain.notification.dao.NotificationDao">

    <!-- 공통 컬럼 (snake_case → DTO camelCase 자동 매핑) -->
    <sql id="cols">
        id, user_id, weekly_budget_id, threshold,
        current_budget, spent_money, ratio, is_read, created_at
    </sql>

    <!-- 5-1. 목록 (isRead 주면 필터, 없으면 전체) 최신순 -->
    <select id="selectByUser" resultType="Notification">
        SELECT <include refid="cols"/>
        FROM notification
        WHERE user_id = #{userId}
        <if test="isRead != null">
            AND is_read = #{isRead}
        </if>
        ORDER BY created_at DESC
    </select>

    <!-- 5-2. 안읽은 개수 -->
    <select id="countUnread" resultType="int">
        SELECT COUNT(*)
        FROM notification
        WHERE user_id = #{userId}
          AND is_read = 0
    </select>

    <!-- 5-3. 단건 읽음 처리 -->
    <update id="markRead">
        UPDATE notification SET is_read = 1
        WHERE id = #{id}
    </update>

    <!-- 5-4. 전체 읽음 처리 (안 읽은 것만) -->
    <update id="markAllRead">
        UPDATE notification SET is_read = 1
        WHERE user_id = #{userId}
          AND is_read = 0
    </update>

    <!-- 보조: id 단건 (존재 404 + 소유권 403 확인용) -->
    <select id="selectById" resultType="Notification">
        SELECT <include refid="cols"/>
        FROM notification
        WHERE id = #{id}
    </select>

</mapper>