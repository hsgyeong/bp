<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bp.jaringochi.domain.report.dao.ReportDao">

    <!-- 유저 + 연/월 단건 (없으면 null) -->
    <select id="selectByUserAndMonth" resultType="MonthlyReport">
        SELECT id, user_id, report_year, report_month,
               total_expense, prev_expense, diff_ratio,
               success_weeks, total_weeks, top_category, category_json,
               one_liner, mood, category_comment, advice,
               user_message, gulbi_reply, replied_at, generated_at
        FROM monthly_report
        WHERE user_id = #{userId}
          AND report_year = #{year}
          AND report_month = #{month}
    </select>

    <!-- 생성 저장 (id 자동 채움) -->
    <insert id="insert" parameterType="MonthlyReport"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO monthly_report
            (user_id, report_year, report_month,
             total_expense, prev_expense, diff_ratio,
             success_weeks, total_weeks, top_category, category_json,
             one_liner, mood, category_comment, advice)
        VALUES
            (#{userId}, #{reportYear}, #{reportMonth},
             #{totalExpense}, #{prevExpense}, #{diffRatio},
             #{successWeeks}, #{totalWeeks}, #{topCategory}, #{categoryJson},
             #{oneLiner}, #{mood}, #{categoryComment}, #{advice})
    </insert>

    <!-- "굴비에게 한 마디" 응답 저장 (월 1회) -->
    <update id="updateReply">
        UPDATE monthly_report
        SET user_message = #{userMessage},
            gulbi_reply  = #{gulbiReply},
            replied_at   = #{repliedAt}
        WHERE id = #{id}
    </update>

</mapper>
