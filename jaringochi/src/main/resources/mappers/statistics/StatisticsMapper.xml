<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bp.jaringochi.domain.statistics.dao.StatisticsDao">

    <!-- 6-1. 카테고리별 집계 (금액 내림차순). type 없으면 수입+지출 전체 -->
    <select id="selectByCategory" resultType="CategoryStatItem">
        SELECT
            c.id   AS categoryId,
            c.name AS categoryName,
            COALESCE(SUM(t.amount), 0) AS amount
        FROM `transaction` t
        JOIN category c ON c.id = t.category_id
        WHERE t.user_id = #{userId}
          AND t.`date` BETWEEN #{startDate} AND #{endDate}
        <if test="type != null">
          AND t.type = #{type}
        </if>
        GROUP BY c.id, c.name
        ORDER BY amount DESC
    </select>

    <!-- 6-2. 기간 수입/지출 합계 (balance는 서비스에서 income - expense) -->
    <select id="selectSummary" resultType="StatisticsSummary">
        SELECT
            COALESCE(SUM(CASE WHEN t.type = 1 THEN t.amount END), 0) AS income,
            COALESCE(SUM(CASE WHEN t.type = 2 THEN t.amount END), 0) AS expense
        FROM `transaction` t
        WHERE t.user_id = #{userId}
          AND t.`date` BETWEEN #{startDate} AND #{endDate}
    </select>

</mapper>